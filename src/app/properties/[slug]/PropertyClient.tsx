/**
 * LEGACY — no longer used.
 * The property detail page was rebuilt at src/components/property/PropertyDetail.tsx
 * (new SCF field structure, July 2026). This stub remains only because the file
 * is still type-checked by the build; it renders nothing and can be deleted
 * whenever convenient.
 */
export default function PropertyClientLegacy() {
  return null;
}
